datasources:
  default:
    url: jdbc:h2:mem:testefretegrpc
    driverClassName: org.h2.Driver
    username: sa
    password:
jpa.default.properties.hibernate.show_sql: true
jpa.default.properties.hibernate.format_sql: true